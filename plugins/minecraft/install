#!/usr/bin/env bash

set -euo pipefail

source "$DOTF/framework"

SPIGOT_ROOT="/home/minecraft/spigot"
SPIGOT_REV="$(cat "$DOTF/plugins/minecraft/VERSION")"
SPIGOT_JAR_FILENAME="spigot-${SPIGOT_REV}.jar"
SPIGOT_JAR_URL="https://hub.spigotmc.org/jenkins/job/BuildTools/lastSuccessfulBuild/artifact/target/BuildTools.jar"
SPIGOT_TMP="$TMP/spigot-${SPIGOT_REV}"

main() {
  create_group minecraft
  add_user_to_group minecraft
  create-minecraft-user

  if ! is-spigot-server-installed; then
    install-spigot-server
  fi

  # install-server
  # copy-server-properties
  # setup-service
}

create-minecraft-user() {
  bullet 'Creating minecraft user... '
  if user_exists minecraft; then
    info 'already exists.'
    return
  else
    sudo useradd minecraft --create-home -g minecraft && success 'done'
  fi
}

is-spigot-server-installed() {
  [ -e "/home/minecraft/spigot/${SPIGOT_JAR_FILENAME}" ]
}

install-spigot-server() {
  build-spigot-server

  bullet 'Installing spigot server...'
  as-minecraft mkdir -p "$SPIGOT_ROOT"
  sudo cp "${SPIGOT_TMP}/${SPIGOT_JAR_FILENAME}" "$SPIGOT_ROOT/"
  sudo cp "$DOTF/plugins/minecraft/VERSION" "$SPIGOT_ROOT/VERSION"
  sudo cp "$DOTF/plugins/minecraft/spigot-start" "$SPIGOT_ROOT/start"
  sudo bash -c "chown minecraft:minecraft ${SPIGOT_ROOT}/*"
  sudo chmod 750 "${SPIGOT_ROOT}/start"

  install-service $DOTF/plugins/minecraft/spigotmc.service.erb system

  "$DOTF/plugins/minecraft/install-spigot-server-properties"
}

build-spigot-server() {
  bullet 'Building spigot server...'

  # Build it
  mkdir -p "${SPIGOT_TMP}"
  cd "${SPIGOT_TMP}"

  if [ ! -e BuildTools.jar ]; then
    curl "$SPIGOT_JAR_URL" -o BuildTools.jar
  fi

  if [ ! -e "$SPIGOT_JAR_FILENAME" ]; then
    java -jar BuildTools.jar --rev "${SPIGOT_REV}"
  fi

  success 'done.'
}

install-server() {
  sudo --login -u minecraft curl "$JAR_URL" -o "$JAR_FILENAME"
  sudo --login -u minecraft "sed -i s/eula=.*/eula=TRUE/g"
}

as-minecraft() {
  sudo --login -u minecraft "$@"
}

copy-server-properties() {
  sudo cp -f $DOTF/plugins/minecraft/server.properties /home/minecraft/
  sudo chown minecraft:minecraft /home/minecraft/server.properties
}

setup-service() {
  install-service $DOTF/plugins/minecraft/minecraft.service.erb system

  systemctl daemon-reload
  systemctl start minecraft
}

main "$@"
