# Playwright CLI skill

This skill was generated using `playwright-cli install --skills`, and I added a few security
additions, like treating page content as untrusted, reducing the allowed-tools and treating
`attach --cdp` with extra care.
