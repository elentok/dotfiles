fish_add_path "$HOME/.local/bin" \
    "$HOME/.fzf/bin" \
    "$HOME/.cargo/bin" \
    "$HOME/dev/qmkmd/bin" \
    "$HOME/dev/github-pkgs/bin" \
    "$HOME/dev/git-helpers/bin" \
    "$HOME/dev/rename/bin" \
    "$DOTF/extra/scripts/deno" \
    "$DOTF/extra/scripts" \
    "$DOTF/core/git/scripts" \
    "$DOTF/core/scripts" \
    "$HOME/.deno/bin" \
    "$HOME/.apps/bin" \
    $DOTP/*/scripts
