Rails Ubuntu Installer
======================

1. Run `as-root` as root (it will install the required apt packages).
2. Run `as-rails` as rails (it will install rbenv, ruby-build and ruby 2.0.0-p247)
