#!/usr/bin/env bash

source `dirname $0`/../../framework

main() {
  header "SSH"
  install_symlinks
}

install_symlinks() {
  symlink "$DOTF/extra/ssh/rc" ~/.ssh/rc
  symlink "$DOTF/extra/ssh/config" ~/.ssh/config
}

main "$@"

