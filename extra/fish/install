#!/usr/bin/env bash

source $DOTF/framework

function main() {
  if command_missing fish; then
    dotf-apt fish
    # $DOTF/extra/fish/build
  fi

  symlink "$DOTF/extra/fish" "$HOME/.config/fish"
}

main "$@"

