# AI Rules

- Read .ai/workflow.md

## Bash scripts rules

- When do run `shfmt` to format and `shellcheck` for linting (fix any issues you find)
- Prefer `if [[ ]]; then ... fi` over `[[ ]] && ...`
- Prefer `echo` to `printf`
